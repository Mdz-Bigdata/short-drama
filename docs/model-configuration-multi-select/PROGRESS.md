# 进度

- [x] 核对 ElevenLabs 官方模型与能力边界
- [x] 移除保存请求的 50 项业务上限
- [x] 实现批量选择与批量保存
- [x] 实现 14 类能力筛选与独立服务说明
- [x] 实现逐模型删除 API、审计与运行时刷新
- [x] 实现已保存模型叉号删除
- [x] 后端回归测试通过
- [x] 前端测试、类型检查、Lint 与生产构建通过
- [x] 全量回归与密钥泄露扫描通过

验证结果：后端 `172 passed`（另含 18 个子测试），前端 `6 passed / 10 tests`，ESLint 与生产构建通过；用户提供的 Key 前缀未出现在工作区文件中。

## 2026-08-15：突破 5 项显示问题

- [x] 已保存区域改为完整模型数据源，不做截断或分页。
- [x] 增加独立滚动容器，固定页脚不再遮住第 6 项及后续条目。
- [x] 增加总保存数与启用数的显式统计。
- [x] 前端以 8 项保存数据验证全部 8 项均存在于列表。
- [x] 后端以 8 项批量保存验证保存响应、列表响应和统计均返回 8。

本次全量结果：后端 `173 passed`（另含 18 个子测试），前端 `6 passed / 11 tests`，ESLint、TypeScript、生产构建及 `git diff --check` 全部通过。

## 2026-08-15：首页配置状态同步

- [x] 首页徽标改为通过后端绝对地址读取 `/api/model-configurations` 的权威全局启用状态。
- [x] 登录、保存、删除与关闭配置窗口时刷新状态。
- [x] 存在任意已启用模型显示“模型：已配置”；全部停用或删除后显示“模型：未配置”。
- [x] 保存、启用、停用、删除后自动刷新，并将各分类默认启用模型注入空任务配置。
- [x] 新增首页启用/未启用状态测试，并验证配置中心所有状态变更都会发出刷新通知。

验证结果：前端 `7 passed / 12 tests`，ESLint、TypeScript、生产构建及 `git diff --check` 全部通过。
