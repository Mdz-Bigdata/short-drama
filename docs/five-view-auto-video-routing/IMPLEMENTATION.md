# 五视图与自动视频路由实施计划

## 阶段 1：模板与契约

- [x] 把指定三视图模板完整改写为五视图。
- [x] 固定五视图顺序、画布、身份一致性和负面约束。
- [x] 定义分镜到视频的不可漂移镜头契约与指纹。

## 阶段 2：能力注册与自动判断

- [x] 扩展视频供应商能力档案，区分已接通、需配置和待核实状态。
- [x] 实现约束优先、可解释的视频模式/供应商评分器。
- [x] 支持模型别名，包括将用户输入的 `Gork` 归一化为 `grok`。
- [x] 对不兼容模式执行显式降级或失败关闭。

## 阶段 3：流水线接入

- [x] 九宫格每格生成共享镜头契约。
- [x] 分镜图片提示词和运镜提示词从同一契约编译。
- [x] Stage 5 使用自动决策结果提交视频，并保存模式、供应商、理由和指纹。
- [x] API/前端展示自动判断含义和实际使用结果。

## 阶段 4：验证

- [x] 增加自动路由、能力协商和指纹防漂移测试。
- [x] 增加文档五视图/九宫格规范测试。
- [x] 运行后端测试、Ruff、前端测试、ESLint、类型检查与生产构建。
- [x] 执行密钥扫描、代码审查和安全复核。

## 完成定义

代码不以“支持列表”代替真实行为：只有具备数据契约、运行时能力协商、生成路径、可追溯输出和回归测试的能力才算完成。外部供应商未配置时，离线实现应给出明确的不可提交原因，而不是伪造成功。
